<style scoped>
/* * {
    margin: 0;
    padding: 0;
}
.nav {
    margin: 0.5rem 0;
    padding-left: 1rem;
    display: flex;
    justify-content: flex-end;
}

ul {
    margin: 0;
    display: flex;
    flex-direction: row;
    list-style: none;
}

li, li > a {
    display: flex;
    flex-direction: row;
    align-items: center;
}
.router-link-active {
    color: var(--sdfi_action);
    box-shadow: 0 -4px 0 var(--sdfi_action) inset;
}
a {
    padding: 0.5rem 1rem;
    height: 2rem !important;
}
h6 {
    height: 2rem !important;
    color: var(--sdfi_transparentBlack);
    white-space: nowrap;
    word-break: keep-all;
    flex-shrink: 0;
    padding: 0.5rem;
}

@media only screen and (max-width: 880px) {
    .menuDropDown {
        position: absolute;
        display: block;
        z-index: 3;
        background: var(--sdfi_white);
        margin: 3.5rem 0 0 0;
        border: 1px solid var(--sdfi_action);
    }
    h6 {
        padding: 0;
        margin: 0.75rem 0.75rem 0.75rem 0;
    }
    li > a {
        width: 100%;
        padding: 0;
        margin: 0.5rem 0.5rem 0.5rem 0;
    }
    ul {
        display: none;
    }
} */
</style>
